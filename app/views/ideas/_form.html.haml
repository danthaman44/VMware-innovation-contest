= form_for @idea do |f|

  .row
    .span12
      = render partial: "globals/form_errors", locals: { object: @idea }

  .row
    .span2
      = f.label :title, class: "required"
    .span2
      = f.text_field :title
  .row
    .span2
      = f.label :description, class: "required"
    .span2
      = f.text_field :description
  .row
    .span2
      = f.label :user_id, class: "required"
    .span2
      = f.number_field :user_id
  .row
    .span2
      = f.label :published, class: "required"
    .span2
      = f.check_box :published

  .row
    .span12
      = render partial: "globals/form_buttons",
                        locals: { f: f, cancel_path: ideas_path, object: @idea }
